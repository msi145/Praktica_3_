<Window x:Class="WmsClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WMS — Система управления складом" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="15,4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#22272E">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="WMS Склад" Foreground="White" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <Button Content="🧪 Готовая продукция" Style="{StaticResource NavButton}" Click="NavProducts_Click"/>
                <Button Content="📦 Складское сырьё" Style="{StaticResource NavButton}" Click="NavRawMaterials_Click"/>
                <Button Content="⚙️ Оборудование" Style="{StaticResource NavButton}" Click="NavEquipment_Click"/>
                <Button Content="🏢 Цеха" Style="{StaticResource NavButton}" Click="NavDepartments_Click"/>
                <Button Content="👥 Пользователи" Style="{StaticResource NavButton}" Click="NavUsers_Click"/>
                <Button Content="🤝 Поставщики" Style="{StaticResource NavButton}" Click="NavSuppliers_Click"/>
            </StackPanel>
        </Border>

        <ContentControl x:Name="MainContent" Grid.Column="1" Background="#F5F6F8"/>
    </Grid>
</Window>
